// Example usage in a JavaScript context
let defaultSymbolSize = 30;

// Later in the code, you might use this variable to set the size of symbols/icons
function drawSymbol() {
    let size = defaultSymbolSize;
    // Use 'size' to draw a symbol with the default size
}
